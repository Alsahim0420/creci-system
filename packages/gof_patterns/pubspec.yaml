name: gof_patterns
description: "Patrones de diseño GoF (creacionales, estructurales y comportamentales) en Dart puro."
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.6.0

resolution: workspace

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
